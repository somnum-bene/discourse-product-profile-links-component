import { describe, expect, it } from "vitest";

// Proves the unit-test runner actually executes. Replaced by real coverage of
// the Profile Links resolution module in the next ticket.
describe("unit test runner", () => {
  it("runs", () => {
    expect(true).toBe(true);
  });
});
